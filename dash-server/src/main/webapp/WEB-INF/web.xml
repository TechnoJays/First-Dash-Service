<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://java.sun.com/xml/ns/javaee" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://java.sun.com/xml/ns/javaee http://java.sun.com/xml/ns/javaee/web-app_3_0.xsd"
        version="3.0">
    <!--
    Currently, the FIRST Dash Application is configured xml-less, so the configuration options below are unnecessary.
    The same configuration is achieved simply in org.technojays.first.DashAppConfig through @ApplicationPath annotation.
    If a more sophisticated (read complicated) setup is needed where security needs to managed across the web
    application, then using this web.xml will be a must.

    <servlet>
        <servlet-name>org.technojays.first.DashAppConfig</servlet-name>
        <load-on-startup>1</load-on-startup>
    </servlet>

    <servlet-mapping>
        <servlet-name>org.technojays.first.DashAppConfig</servlet-name>
        <url-pattern>/api/v1/*</url-pattern>
    </servlet-mapping>
    -->
</web-app>
